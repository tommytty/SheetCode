[project]
name = "sheetcode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tommy", email = "tommytty@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sheetcode = "sheetcode:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
